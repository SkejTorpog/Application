<Window x:Class="WpfApp6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp6"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0" ItemsSource="{Binding Functions}" SelectedValue="{Binding SelectedFunction, Mode=TwoWay}">

        </ListBox>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Аргумент А"></TextBlock>
            <TextBox Text="{Binding Path=SelectedModel.A,UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBlock Text="Аргумент Б"></TextBlock>
            <TextBox Text="{Binding Path=SelectedModel.B}"></TextBox>
            <TextBlock Text="Аргумент С"></TextBlock>
            <ComboBox ItemsSource="{Binding Сoefficients}" SelectedValue="{Binding Path=SelectedModel.C}">
            </ComboBox>
            <Button Command="{Binding Command}" Height="20"></Button>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding ModelsList}" SelectedItem="{Binding SelectedModel}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Fxy, Mode=TwoWay}"  Header="F(x,y)"></DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding X, Mode=TwoWay}" Header="x" IsReadOnly="false"/>
                <DataGridTextColumn Binding="{Binding Y, Mode=TwoWay}" Header="y" IsReadOnly="false"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>
